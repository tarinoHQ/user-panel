import React, { Component, PropTypes } from 'react';
import c from 'rc-classnames';

class BarIconBtn extends Component {
  render() {
    return (
      <div className={c('')}>

      </div>
    );
  }
}

export default BarIconBtn;
